[package]
name = "murmur-cold"
version = "0.1.0"
edition = "2021"
description = "Cold-storage gossip fanout to trusted contacts for murmur"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
