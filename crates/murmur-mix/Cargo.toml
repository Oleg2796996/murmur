[package]
name = "murmur-mix"
version = "0.1.0"
edition = "2021"
description = "Lightweight mixnet overlay (drop+cover) for murmur"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
